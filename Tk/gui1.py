from tkinter import *

# Intialize tkinter
root = Tk()

# Creates a Label
add_label = Label(root, text="Initial tkinter window screen")
# Pack the widget into the window screen
add_label.pack()

root.mainloop()
